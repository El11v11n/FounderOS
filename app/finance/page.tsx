import { ModulePlaceholder } from "@/components/module-placeholder";

export default function FinancePage() {
  return (
    <ModulePlaceholder
      label="06 // FINANCE"
      title="The numbers."
      description="Net worth, income and expenses with category breakdowns, transactions — quick-capture via Telegram and Revolut CSV import."
      phase={4}
    />
  );
}
