import { ModulePlaceholder } from "@/components/module-placeholder";

export default function JournalPage() {
  return (
    <ModulePlaceholder
      label="09 // JOURNAL"
      title="The log."
      description="Daily three-line entry, habits with streaks, mood tracking with trend — the quiet record behind the numbers."
      phase={2}
    />
  );
}
