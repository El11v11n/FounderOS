import { ModulePlaceholder } from "@/components/module-placeholder";

export default function BrainPage() {
  return (
    <ModulePlaceholder
      label="03 // BRAIN"
      title="The idea inbox."
      description="Every captured idea lands here — tagged, filterable, never lost. One tap converts an idea into a task or project."
      phase={2}
    />
  );
}
