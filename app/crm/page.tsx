import { ModulePlaceholder } from "@/components/module-placeholder";

export default function CrmPage() {
  return (
    <ModulePlaceholder
      label="04 // CRM"
      title="The network."
      description="Contacts with warmth, last contact, next action and follow-up cadence — with overdue flags so no relationship goes cold."
      phase={2}
    />
  );
}
